name: example
description: An example project for the package ffr_hex_color.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  ffr_hex_color:
      path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:  
  uses-material-design: true
